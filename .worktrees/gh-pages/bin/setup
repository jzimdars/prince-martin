#!/usr/bin/env bash
set -euo pipefail

if ! command -v mise >/dev/null 2>&1; then
  echo "mise is not installed. Install it first (e.g., brew install mise)." >&2
  exit 1
fi

mise trust
mise install
mise exec -- bundle install

echo "Setup complete. Run bin/dev to start the server."
